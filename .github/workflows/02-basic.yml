name: second workflow.

on: 
  push: 
    branches: 
      - test
      - master

jobs: 
  second-workflow:
    runs-on: ubuntu-latest
    steps: 
      - name: first step
        run: echo "This workflow will trigger automatically."
