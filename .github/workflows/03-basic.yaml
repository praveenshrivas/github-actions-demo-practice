name: Third Workflow.

on: 
  workflow_dispatch:
  push:
    branches: 
      - main
      - test
  pull_request:
    branches: 
      - main
      - test

jobs:
  runs-on: ubuntu-latest
  steps:
    - name: First Step
      run: echo "Hello World!!!!"
