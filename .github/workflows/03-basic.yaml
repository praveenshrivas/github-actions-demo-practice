name: Third Workflow.

on: 
  workflow_dispatch:
  push:
    branches: 
      - master
      - test
  pull_request:
    branches: 
      - master
      - test

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        run: echo "Hello World!!!!"
      - run: echo "Hello There!!!"
